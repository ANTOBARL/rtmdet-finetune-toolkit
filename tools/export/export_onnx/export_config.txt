# =============================================================================
# ONNX Export Configuration
# Edit this file, then run:
#   python export_onnx.py
# from any working directory.
# =============================================================================

[checkpoint]
# Absolute path to the .pth checkpoint to export.
# Leave empty to auto-detect the best checkpoint using project_dir + model_name below.
checkpoint_path =

# Used only when checkpoint_path is empty — looks for best_coco_bbox_mAP_*.pth inside
# <project_dir>/<model_name>/
project_dir = C:\Users\AntonioreneèBarletta\Desktop\train_RTM_det\runs\rtmdet
model_name  = rtmdet_fire_flame_640_s


[output]
# Folder where end2end.onnx, pipeline.json and deploy.json will be written.
# Leave empty → <checkpoint_dir>/export_onnx/
work_dir =


[model]
# Input image size in pixels. Leave empty to auto-detect from the run manifest.
imgsz =

# Inference device.
device = cuda:0


[onnx]
# Score threshold baked permanently into the ONNX graph.
# Detections below this value are discarded before NMS.
# Cannot be lowered at inference without re-exporting.
score_threshold = 0.05

# IoU threshold for Non-Maximum Suppression baked into the graph.
iou_threshold = 0.5

# Maximum number of detections per image (defines the fixed output tensor size).
keep_top_k = 10

# Run a quick ONNXRuntime inference check after export.
validate = True


[paths]
# Absolute path to the MMDeploy clone. Required.
mmdeploy_root = C:\Users\AntonioreneèBarletta\Desktop\train_RTM_det\mmdeploy

# Absolute path to the MMDetection clone. Used to extend PYTHONPATH.
mmdet_root = C:\Users\AntonioreneèBarletta\Desktop\train_RTM_det\mmdetection

# Sample image for MMDeploy graph tracing.
# Leave empty to auto-detect from the dataset recorded in the run manifest.
sample_image =
