# ==============================================================================
# Requirements — train-rtmdet
# Hardware: NVIDIA GPU  |  CUDA 11.8  |  PyTorch 2.2
#
# NOTE: torch 2.1.0+cu118 was removed from the PyTorch index.
# Bumped to 2.2.2+cu118 — the nearest available stable release.
# mmcv find-links URL updated accordingly (torch2.2.0 bucket).
#
# Install order (run from the repo root with the target conda/venv active):
#
#   Step 1 — all pip-installable packages (torch, mmengine, onnx, …):
#       pip install -r requirements.txt
#
#   Step 2 — MMDetection (local clone):
#       pip install -e mmdetection/
#
#   Step 3 — this package:
#       pip install -e .
# ==============================================================================


# ── PyTorch GPU — CUDA 12.6 ───────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.6.0+cu126
torchvision==0.21.0+cu126
torchaudio==2.6.0+cu126


# ── OpenMMLab core ────────────────────────────────────────────────────────────
mmengine>=0.10.0,<1.0
setuptools>=68.2.2,<70         # keep below 70 to avoid pkg_resources removal issues

# mmcv is not on PyPI as a Linux binary.  The wheel is served from the OpenMMLab CDN.
# The find-links URL encodes the exact (CUDA, Torch) pair — keep in sync with torch above.
--find-links https://download.openmmlab.com/mmcv/dist/cu126/torch2.6.0/index.html
mmcv==2.2.0

# mmdet is installed from the local clone in step 2 above.


# ── ONNX export & validation ──────────────────────────────────────────────────
onnx>=1.16.0
onnxruntime-gpu>=1.18.0   # CUDA 12.x support
onnx-simplifier>=0.4.36   # graph simplification used during export


# ── Image / data processing ───────────────────────────────────────────────────
numpy>=1.24,<2.0           # numpy 2.x breaks several mmdet/mmcv ops
opencv-python-headless>=4.8.0
Pillow>=10.0
scipy>=1.11
matplotlib>=3.7


# ── Config / utilities ────────────────────────────────────────────────────────
pyyaml>=6.0
aenum>=3.1.0                   # required by mmdeploy (ONNX/TensorRT export)
