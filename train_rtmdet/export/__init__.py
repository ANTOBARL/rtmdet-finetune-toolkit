# TensorRT export is currently IDLE.
# To export a trained checkpoint to ONNX, run export_rtmdet_to_onnx.py from the project root.
# To export to TensorRT, see export_rtmdet_to_tensorrt.py (requires MMDeploy + TensorRT SDK).
